name: Build and Deploy to Firebase

on:
  workflow_dispatch:
    inputs:
      configuration:
        description: 'Build configuration'
        required: true
        default: 'production'
        type: choice
        options:
          - production
          - development

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: |
          rm -rf node_modules package-lock.json
          npm install --legacy-peer-deps

      - name: Create environment files
        env:
          ENVIRONMENT_TS_B64: ${{ secrets.ENVIRONMENT_TS_B64 }}
          ENVIRONMENT_PROD_TS_B64: ${{ secrets.ENVIRONMENT_PROD_TS_B64 }}
        run: |
          mkdir -p src/environments
          printf '%s' "$ENVIRONMENT_TS_B64" | base64 --decode > src/environments/environment.ts
          printf '%s' "$ENVIRONMENT_PROD_TS_B64" | base64 --decode > src/environments/environment.prod.ts

      - name: Verify environment files
        run: |
          echo "=== environment.ts ==="
          head -3 src/environments/environment.ts
          echo ""
          echo "=== environment.prod.ts ==="
          head -3 src/environments/environment.prod.ts

      - name: Build (production)
        if: ${{ github.event.inputs.configuration == 'production' }}
        run: npm run build-production

      - name: Build (development)
        if: ${{ github.event.inputs.configuration == 'development' }}
        run: npm run build-debug

      - name: Deploy to Firebase Hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          channelId: live
          projectId: setlisthelperfirebase
